"""Application entry point.

Builds the database connection and launches the Tk main window. The GUI is
added in a later build step; for now this only confirms the package runs.
"""


def main() -> None:
    """Start the Work Shift Manager application."""
    print("Work Shift Manager: scaffolding in place, GUI not built yet.")


if __name__ == "__main__":
    main()
